一个驱动，可以支持多个设备
	每个设备内的数据块在内存中都是以链表的形式来存放的
	如何和proc交互
		


static struct seq_operations scull_seq_ops = {
	.start = scull_seq_start,
	.next  = scull_seq_next,
	.stop  = scull_seq_stop,
	.show  = scull_seq_show
};
