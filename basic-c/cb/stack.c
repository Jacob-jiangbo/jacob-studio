/*
栈stack是一种受限线性表，插入和删除必须在同一端。
基本操作：
创建create，建一个空栈，有效元素个数为0；
清空clear，把栈变成空栈，废弃所有有效元素；
栈顶top，取得最后入栈的那个元素；
入栈push，把一个元素加入到栈中成为新的栈顶；
出栈pop，把栈顶元素从栈中删除（废弃掉）；
有效元素个数size，取得栈中有效元素个数；
是否为空empty，判断栈是否是空栈，一般在出栈和取栈顶之前判断
是否满full，判断栈是否已满，一般在入栈前判断
*/
